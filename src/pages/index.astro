---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const przemyslenia = (await getCollection('przemyslenia')).map(p => ({ ...p, topic: 'przemyślenia' }));
const kod = (await getCollection('kod')).map(p => ({ ...p, topic: 'kod' }));
const allPosts = [...przemyslenia, ...kod];

const recentPosts = [...allPosts]
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const topics = [
  { name: 'przemyślenia', slug: 'przemyslenia', icon: '💭', count: przemyslenia.length },
  { name: 'kod', slug: 'kod', icon: '💻', count: kod.length },
];

const statusIcons = { seed: '🌱', growing: '🌿', evergreen: '🌲' };
---

<BaseLayout title="jasisz">
  <div class="garden">
    <header class="welcome">
      <h1 class="welcome-title">Cyfrowy ogród</h1>
      <p class="welcome-text">
        Miejsce, gdzie sadzę i pielęgnuję myśli. Niektóre dopiero kiełkują
        <span class="status-hint">🌱</span>, inne wciąż rosną
        <span class="status-hint">🌿</span>, a jeszcze inne są już dojrzałe
        <span class="status-hint">🌲</span>.
      </p>
    </header>

    <section class="section">
      <h2 class="section-title">Ostatnio sadzone</h2>
      <ul class="recent-notes">
        {recentPosts.map((post) => (
          <li>
            <a class="note-link" href={`/${post.slug}`}>
              <span class="note-status">{statusIcons[post.data.status]}</span>
              <span class="note-title">{post.data.title}</span>
              {post.data.lang === 'en' && <span class="note-lang">🇬🇧</span>}
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section class="section">
      <h2 class="section-title">Ścieżki w ogrodzie</h2>
      <ul class="paths">
        {topics.map((topic) => (
          <li>
            <a class="path-link" href={`/${topic.slug}`}>
              <span class="path-icon">{topic.icon}</span>
              <span class="path-name">{topic.name}</span>
              <span class="path-count">{topic.count}</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    <nav class="garden-nav">
      <a href="/wszystko">wszystko</a>
      <span class="nav-sep">·</span>
      <a href="/losowa" class="random-link">🎲 losowa</a>
    </nav>
  </div>
</BaseLayout>

<style>
  .garden {
    padding: 2.5rem 0;
  }

  .welcome {
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 1px dashed var(--grey-color-light);
  }

  .welcome-title {
    font-family: Georgia, "Times New Roman", serif;
    font-size: 1.75rem;
    font-weight: 400;
    margin-bottom: 0.75rem;
    color: var(--brand-color);
  }

  .welcome-text {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-color);
    max-width: 480px;
  }

  .status-hint {
    font-size: 0.95rem;
  }

  .section {
    margin-bottom: 2.5rem;
  }

  .section-title {
    font-family: Georgia, "Times New Roman", serif;
    font-size: 0.85rem;
    font-weight: 400;
    color: var(--grey-color);
    text-transform: lowercase;
    letter-spacing: 0.03em;
    margin-bottom: 1rem;
  }

  .recent-notes {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
  }

  .note-link {
    display: inline-flex;
    align-items: baseline;
    gap: 0.5rem;
    text-decoration: none;
    color: inherit;
  }

  .note-status {
    font-size: 0.9rem;
  }

  .note-title {
    font-family: Georgia, "Times New Roman", serif;
    font-size: 1.1rem;
    color: var(--text-color);
    border-bottom: 1px solid transparent;
    transition: border-color 0.15s ease;
  }

  .note-link:hover .note-title {
    border-bottom-color: var(--brand-color);
  }

  .paths {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .path-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.5rem 0.9rem;
    border-radius: 20px;
    text-decoration: none;
    color: inherit;
    background: rgba(107, 124, 94, 0.08);
    transition: all 0.15s ease;
  }

  .path-link:hover {
    background: rgba(107, 124, 94, 0.15);
    text-decoration: none;
  }

  .path-icon {
    font-size: 1rem;
  }

  .path-name {
    font-size: 0.95rem;
    color: var(--text-color);
  }

  .path-count {
    font-size: 0.8rem;
    color: var(--grey-color);
  }

  .garden-nav {
    padding-top: 1rem;
    border-top: 1px dashed var(--grey-color-light);
  }

  .garden-nav a {
    font-size: 0.9rem;
    color: var(--grey-color);
  }

  .garden-nav a:hover {
    color: var(--brand-color);
  }

  .nav-sep {
    color: var(--grey-color-light);
  }

  .random-link {
    color: var(--brand-color);
  }
</style>
